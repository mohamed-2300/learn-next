export const metadata = {
    title:"article - side"
};

const Layout = ({ children }) => {
    
    return(

        <div>
            <h2>this is Layout of article</h2>
            {children}

        </div>
    )
}

export default Layout;